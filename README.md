## ASSIGNMENT (SwiftCart E-Commerce)

---
## 🛍️ API Endpoints
---
1. Get 🛍️ All Products
```bash
https://fakestoreapi.com/products
```

2. Get 🛍️ All Categories
```bash
https://fakestoreapi.com/products/categories
```

3. Get 🛍️ Products by Category
```bash
https://fakestoreapi.com/products/category/${category}
```

Example:
```bash
https://fakestoreapi.com/products/category/jewelery
```

4. Get 🛍️ Single Product Detail
```bash
https://fakestoreapi.com/products/${id}
```

Example:
```bash
https://fakestoreapi.com/products/1
```

---

## 🎯 Project Specifications (UI/UX)

#### 1) Navbar
- Website **logo/name** ("SwiftCart") on the **left**
- **Menu items** (Home, Products, About, Contact) in the **center**
- **Cart Icon/Button** on the **right** (showing item count is a bonus)

#### 2) Banner / Hero Section
- A **background image** (related to shopping/fashion/electronics)
- A **title** (e.g., "Best Collection For You") and **subtitle**
- A **centered button** (e.g., "Shop Now")

#### 3) Features / Why Choose Us
- **Section heading**
- **3-4 items** highlighting features like "Fast Delivery", "24/7 Support", "Secure Payment", etc. (Icon + Title + Short Text)

#### 4) Trending / Top Rated Section
- Show **3 top-rated products** (you can filter by rating or just pick the first 3) based on API data or hardcoded for layout practice.

#### 5) Newsletter & Footer
- **Newsletter Subscription Form**: Email input + Subscribe button.
- **Footer** with copyright info, social links, and quick links.

#### 6) Responsiveness
- Website must be **mobile responsive** 

---
#### 7) Create a README file to answer the following questions-

#### 1) What is the difference between `null` and `undefined`?
`undefined` মানে হলো একটি ভেরিয়েবল ডিক্লেয়ার করা হয়েছে কিন্তু তাতে কোনো মান (value) অ্যাসাইন করা হয়নি। অন্যদিকে, `null` হলো একটি অ্যাসাইন করা মান যা নির্দেশ করে যে ভেরিয়েবলটি ইচ্ছাকৃতভাবে খালি বা "কিছুই না" রাখা হয়েছে।

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
`map()` ফাংশন একটি অ্যারের প্রতিটি উপাদানের উপর একটি ফাংশন চালায় এবং সেই ফাংশনের রিটার্ন করা মানগুলো দিয়ে একটি **নতুন অ্যারে** তৈরি করে।

পার্থক্য:
- `map()`: একটি **নতুন অ্যারে** রিটার্ন করে এবং মূল অ্যারে পরিবর্তন করে না। এটি সাধারণত ডেটা ট্রান্সফর্ম বা মডিফাই করার জন্য ব্যবহৃত হয়।
- `forEach()`: কোনো মান রিটার্ন করে না (`undefined` রিটার্ন করে)। এটি শুধুমাত্র অ্যারের প্রতিটি উপাদানের জন্য একটি কাজ (side effect) সম্পাদন করে, যেমন কনসোলে প্রিন্ট করা বা ডেটাবেসে সেভ করা।

#### 3) What is the difference between `==` and `===`?
`==` (লোজ ইকুয়ালিটি) এবং `===` (স্ট্রিক্ট ইকুয়ালিটি) উভয়ই তুলনা করার জন্য ব্যবহৃত হয়, কিন্তু তাদের কাজের ধরন ভিন্ন।

- `==`: এটি শুধুমাত্র **মান (value)** তুলনা করে। যদি দুটিি ভেরিয়েবলের ডেটা টাইপ ভিন্ন হয়, তবে এটি তাদের একই টাইপে রূপান্তর (type coercion) করার চেষ্টা করে এবং তারপর তুলনা করে। উদাহরণস্বরূপ: `5 == "5"` সত্য (true) হবে।
- `===`: এটি **মান (value) এবং ডেটা টাইপ (data type)** উভয়ই তুলনা করে। এখানে কোনো রূপান্তর ঘটে না। উদাহরণস্বরূপ: `5 === "5"` মিথ্যা (false) হবে কারণ একটি সংখ্যা এবং অন্যটি স্ট্রিং। সাধারণত `===` ব্যবহার করাই ভালো অভ্যাস।

#### 4) What is the significance of `async`/`await` in fetching API data?
`async`/`await` হলো প্রমিজ (Promise) হ্যান্ডেল করার একটি আধুনিক এবং পরিষ্কার উপায়। এটি অ্যাসিনক্রোনাস (asynchronous) কোডকে সিনক্রোনাস (synchronous) কোডের মতো দেখতে এবং আচরণ করতে সাহায্য করে।

গুরুত্ব:
- **রিডেবিলিটি (Readability):** `.then()` এবং `.catch()` চেইন ব্যবহার করার চেয়ে `async`/`await` কোড পড়া এবং বোঝা সহজ করে।
- **এরর হ্যান্ডলিং (Error Handling):** `try...catch` ব্লক ব্যবহার করে খুব সহজেই এরর হ্যান্ডেল করা যায়।
- **ডিবাগিং:** কোডের প্রবাহ বোঝা সহজ হওয়ায় ডিবাগ করা সুবিধাজনক হয়। এটি কোডকে "callback hell" থেকে রক্ষা করে।

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
স্কোপ নির্ধারণ করে যে কোডের কোন অংশ থেকে কোন ভেরিয়েবল বা ফাংশন অ্যাক্সেস করা যাবে।

- **Global Scope (গ্লোবাল স্কোপ):** ভেরিয়েবলটি যদি কোনো ফাংশন বা ব্লকের বাইরে ডিক্লেয়ার করা হয়, তবে এটি গ্লোবাল স্কোপে থাকে। এটি কোডের যেকোনো জায়গা থেকে অ্যাক্সেস করা যায়।
- **Function Scope (ফাংশন স্কোপ):** `var` দিয়ে ডিক্লেয়ার করা ভেরিয়েবলগুলো ফাংশন স্কোপড হয়। অর্থাৎ, একটি ফাংশনের ভেতরে ডিক্লেয়ার করা ভেরিয়েবল শুধুমাত্র সেই ফাংশনের ভেতরেই অ্যাক্সেস করা যায়, বাইরে থেকে নয়।
- **Block Scope (ব্লক স্কোপ):** `let` এবং `const` দিয়ে ডিক্লেয়ার করা ভেরিয়েবলগুলো ব্লক স্কোপড হয়। একটি ব্লক হলো `{}` (কোকড়া ব্র্যাকেট) এর মধ্যে থাকা কোড (যেমন `if`, `for` লুপ)। এই ভেরিয়েবলগুলো শুধুমাত্র সেই নির্দিষ্ট ব্লকের ভেতরেই অ্যাক্সেস করা যায়।

